<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.osho.powerweight">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PowerWeight"
        android:fullBackupContent="true">
        <activity
            android:name=".UI.CheckoutActivity"
            android:exported="false" />
        <activity
            android:name=".UI.GraphicsActivity"
            android:exported="false" />
        <activity
            android:name=".UI.AssistanceActivity"
            android:exported="false" />
        <activity
            android:name=".UI.ProfileActivity"
            android:exported="false" />
        <activity android:name=".UI.HealthActivity" />
        <activity android:name=".UI.UpdateProfileData" />
        <activity android:name=".UI.DashBoardActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/SplashTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>